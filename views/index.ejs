<html>
  <body>
    <ul>
        <% for (let i = 0; i < messages.length; i++) { %>
          <li>
            <h3><%= messages[i].user %></h3>
              <p><%= messages[i].text %></p>
              <p><%= messages[i].added %></p>
              <a href="/<%= messages[i].user %>/<%= messages[i].id %>">Open</a>
            </a>
          </li>
        <% } %>
      </ul>
      <a href="/new">Create</a>
  </body>
</html>
